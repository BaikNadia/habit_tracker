# 🧘‍♀️ Habit Tracker — Трекер полезных привычек
Habit Tracker — это REST API на Django, позволяющее пользователям создавать, 
отслеживать и получать напоминания о своих полезных привычках через Telegram.

## 📌 Основные возможности
📝 Создание, редактирование и удаление привычек
🔒 Личный кабинет: пользователи видят только свои привычки
🌍 Публичные привычки: можно просматривать чужие публичные привычки
⏰ Пагинация (5 привычек на страницу)
📱 Интеграция с Telegram: автоматические напоминания
✅ Валидация по бизнес-правилам:
Время выполнения ≤ 120 секунд
Нельзя указывать и вознаграждение, и связанную привычку одновременно
Связанная привычка должна быть «приятной»
Периодичность — от 1 до 7 дней
🔐 Аутентификация через JWT
📚 Документация API через Swagger

## 🛠 Технологии
Backend: Python 3.11, Django 4.2, Django REST Framework
Аутентификация: djangorestframework-simplejwt
Асинхронные задачи: Celery + Redis
Интеграция: Telegram Bot API
Документация: drf-yasg (Swagger)
Тестирование: coverage, flake8
База данных: PostgreSQL

## 🚀 Запуск проекта
1. Клонируйте репозиторий:
git clone https://github.com/BaikNadia/habit_tracker.git
cd habit_tracker

2. Создайте и активируйте виртуальное окружение:
python -m venv .venv
# Windows:
.venv\Scripts\activate
# Linux/macOS:
source .venv/bin/activate

3. Установите зависимости:
pip install -r requirements.txt

4. Настройте переменные окружения:
Создайте файл .env в корне проекта:
SECRET_KEY=ваш-секретный-ключ
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

TELEGRAM_BOT_TOKEN=123456789:ABCdefGhIJKlmNoPQRsTUVwxyZ # TELEGRAM_CHAT_ID можно не указывать — он сохраняется в профиле пользователя

CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

5. Выполните миграции:
python manage.py migrate

6. Создайте суперпользователя:
python manage.py createsuperuser

7.  Запустите сервер и Celery

8. Откройте документацию:
http://127.0.0.1:8000/swagger/

## 📬 Интеграция с Telegram
Создайте бота через @BotFather.
Скопируйте токен и вставьте в .env.
Найдите своего бота в Telegram и нажмите «Start».
Узнайте свой chat_id:
https://api.telegram.org/botВАШ_ТОКЕН/getUpdates

Сохраните chat_id в профиле пользователя (через админку или API).
Создайте привычку с временем в будущем — бот пришлёт напоминание!

## 🐳 Docker

### Сборка образа
```bash
docker build -t your-username/habit-tracker:latest .

### Запуск контейнера:
docker run -d \
  --name habit_tracker \
  -p 80:8000 \
  -e SECRET_KEY='your-secret-key' \
  -e DEBUG=False \
  -e ALLOWED_HOSTS='your-server-ip,localhost,127.0.0.1' \
  -e DB_NAME='your-db-name' \
  -e DB_USER='your-db-user' \
  -e DB_PASSWORD='your-db-password' \
  -e DB_HOST='your-db-host' \
  -e DB_PORT='5432' \
  -e TELEGRAM_BOT_TOKEN='your-telegram-bot-token' \
  -e CELERY_BROKER_URL='redis://your-redis-host:6379/0' \
  -e CELERY_RESULT_BACKEND='redis://your-redis-host:6379/0' \
  your-username/habit-tracker:latest
  
### Публикация в Docker Hub:
docker login
docker push your-username/habit-tracker:latest

## 🚀 CI/CD с GitHub Actions
Проект использует автоматический пайплайн развертывания.
### Workflow stages:
Lint - Проверка кода с flake8

Test - Запуск тестов Django

Build - Сборка Docker образа и публикация в Docker Hub

Deploy - Автоматический деплой на сервер

### Требуемые Secrets в GitHub:
DOCKERHUB_USERNAME - Логин Docker Hub

DOCKERHUB_TOKEN - Токен доступа Docker Hub

DEPLOY_SSH_KEY - Приватный SSH ключ для доступа к серверу

SERVER_IP - IP адрес сервера 

SSH_USER - Пользователь для SSH 

SECRET_KEY - Секретный ключ Django

Остальные переменные окружения для БД и Telegram бота 

## Ручной деплой (если нужно):
# На сервере
docker pull your-username/habit-tracker:latest
docker stop habit_tracker || true
docker rm habit_tracker || true
docker run -d --name habit_tracker -p 80:8000 your-username/habit-tracker:latest 

## Мониторинг:
# Проверка статуса контейнера
docker ps

# Просмотр логов
docker logs habit_tracker -f

# Проверка работы приложения
curl http://158.160.3.35/

## Структура проекта:

habit_tracker/
├── .github/
│   └── workflows/
│       └── deploy.yml              # CI/CD пайплайн GitHub Actions
├── config/                         # Основной проект Django
│   ├── __init__.py
│   ├── settings.py                 # Настройки проекта
│   ├── urls.py                     # Главные URL patterns
│   └── wsgi.py
├── habits/                         # Приложение привычек
│   ├── migrations/
│   │   └── __init__.py
│   ├── __init__.py
│   ├── admin.py                    # Админка для привычек
│   ├── apps.py
│   ├── models.py                   # Модели Habit, Reward
│   ├── serializers.py              # Сериализаторы Django REST
│   ├── tests.py                    # Тесты приложения
│   ├── urls.py                     # API endpoints привычек
│   └── views.py                    # View classes API
├── users/                          # Приложение пользователей
│   ├── migrations/
│   │   └── __init__.py
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py                   # Модель User
│   ├── serializers.py
│   ├── tests.py
│   ├── urls.py
│   └── views.py
├── telegram_bot/                   # Телеграм бот
│   ├── __init__.py
│   ├── apps.py
│   ├── handlers.py                 # Обработчики команд бота
│   ├── models.py
│   ├── tests.py
│   └── views.py
├── static/                         # Статические файлы
│   └── ...
├── staticfiles/                    # Собранные статические файлы
│   └── ...
├── media/                          # Медиа файлы
├── .env                           # Переменные окружения (local)
├── .gitignore
├── docker-compose.yml             # Docker Compose (если есть)
├── Dockerfile                     # Конфигурация Docker образа
├── requirements.txt               # Зависимости Python
├── manage.py                      # Django management script
└── README.md                      # Документация проекта

## Описание ключевых файлов:
Конфигурация Docker:
Dockerfile - инструкции для сборки Docker образа

.dockerignore - файлы, игнорируемые при сборке

CI/CD:
.github/workflows/deploy.yml - автоматический пайплайн деплоя

Django проект:
config/ - основной проект Django с настройками

habits/ - приложение для управления привычками

users/ - приложение пользователей и аутентификации

telegram_bot/ - интеграция с Telegram

Конфигурация:
requirements.txt - список Python зависимостей

.env - переменные окружения для разработки

Управление:
manage.py - скрипт для управления Django приложением

README.md - документация проекта

## 📜 Лицензия:
Проект создан в учебных целях. Разрешается использовать код другим лицам
(с указанием автора)